# AGENTS.md instructions for sellerie

Always make sound and epistemic decisions by justifying them. Do not assume; check and verify.

Use `WORKFLOW.md` as the Entr'acte/Symphony runner contract. Keep Linear issues executable by making the task contract, acceptance criteria, and validation explicit before coding.

## Validation

Run `make validate` when the Makefile is present. If the task introduces a language or framework-specific test command, add it to `make validate` or document the exact command in the PR.
